# --- Tryb A: tunel HTTPS (PWA standalone na Androidzie) — Polecany ---
# Zostaw puste — API idzie przez proxy Vite (/api → backend :4000)
VITE_API_URL=
# Uruchom: backend dev + frontend dev:phone + frontend tunnel
# Otwórz link https://....trycloudflare.com na telefonie → Zainstaluj aplikację

# --- Tryb B: bezpośrednio po IP w LAN (skrót w Chrome, z paskiem URL) ---
# VITE_API_URL="http://192.168.100.4:4000"
# Uruchom: npm run dev:phone
# Telefon: http://192.168.100.4:5173
